---
version: alpha
name: "AURA"
description: "被月光托起的可信塔罗仪式：卡牌永远是第一视觉焦点，解释清楚且不过度神秘化。"
colors:
  night-background: "#100C19"
  mist-purple: "#8C789D"
  moon-white: "#F1ECF5"
  text-muted: "#CEC4D6"
  restrained-gold: "#B6A06D"
typography:
  display-zh:
    fontFamily: '"Source Han Serif SC", "Noto Serif SC", serif'
  display-latin:
    fontFamily: '"Cinzel", "Times New Roman", serif'
  sans:
    fontFamily: '"Source Han Sans SC", "Noto Sans SC", system-ui, sans-serif'
components:
  card-frame:
    targetViewportHeight: "35%-42%"
    fit: "contain"
  tap-target:
    minWidth: "88px"
    minHeight: "88px"
  result-hero:
    readingHint: "one-to-two-line-peek"
omitted:
  - section: spacing
    reason: "Milestone 1 只冻结触控、卡牌和安全区边界；完整间距比例待 Cocos 原型验证后固化。"
  - section: rounded
    reason: "圆角数值尚未由真机视觉验收确定；形状语言先由正文合同约束。"
---

# AURA Design System

## Overview

### Creative North Star

AURA 像一张安静的月光仪式桌：深夜紫蓝退到背景，月白托起当下结果，细铭刻线提供可信秩序。界面帮助玩家专注于卡牌与解释，不把神秘感做成威压、廉价星云或无来源符号堆砌。

### Product context and register

- **用户与任务：** 面向塔罗新手和爱好者，完成抽牌、理解规则解读、反思与保存结果。
- **目标市场：** 中国大陆微信小游戏；证据来自批准的产品与 Milestone 1 规格。
- **语言：** 首发简体中文。英文仅作短副标题、章节编号或牌名辅助；中文信息永远完整可用。
- **使用场景：** 手机竖屏、单手、短时仪式与随后较长阅读。
- **表达范围：** 混合型。抽牌揭示允许品牌表达，输入、长文阅读、保存与错误恢复坚持产品可用性。
- **记忆点：** 十二阶段月相环、可解释的对称几何、卡牌后的柔和月晕和细铭刻分隔线。
- **克制点：** 卡牌以外的一切都必须后退；阅读期间背景和装饰接近静止。
- **禁止参照：** 不做赌场式炫光、恐吓式占卜、持续发光的星云海报、随机咒文或混杂宗教符号。
- **Token 所有权：** 本文件记录批准的视觉身份。运行时主题 Token 与资源槽位必须映射本文件；实现后在同一变更中维护映射，不能形成第二套视觉真相。

## Colors

- `night-background` 是全局最低权重底色，不得承担正文直接承载。
- `mist-purple` 用于柔光、次级面板和低对比氛围，不用于大面积高饱和按钮。
- `moon-white` 用于主标题、卡牌邻近高光和焦点；不能让背景装饰获得同等亮度。
- `text-muted` 用于次级说明，正文仍需通过实际真机对比度检查。
- `restrained-gold` 只用于细铭刻线、少量状态强调和古典秩序层，不能成为常驻发光边框。

Milestone 1 只有深色月光主题。高对比或其他主题必须沿用相同语义层级，不能改变页面结构和操作优先级。

## Typography

- 中文短标题使用思源宋体 SC SemiBold；英文短标题、罗马数字和章节编号使用 Cinzel。
- 正文、按钮和工具信息使用思源黑体 SC 或等价轻量中文黑体。
- 罗马碑铭感只进入短标题、英文和数字层；正文禁止仿古、全大写或过宽字距。
- 中文标题字体按批准文案做字符子集并保留字体许可证；缺失时必须回退系统字体，不能出现空白。
- 长文使用稳定行高和可读宽度，避免把大段解读居中排版。

## Layout

- 基准为 `750 × 1334` 竖屏，适配安全区而非缩放固定截图。
- 主要点击区至少 `88 × 88` 设计像素，主操作优先落在下半屏单手舒适区。
- 结果页大卡面容器占首屏可用高度约 35% 至 42%；主题不能改变其宽高比和正文起始位置。
- 核心结论在首屏可见，详细解读只半露出一至两行并向下展开。
- halo 只能在卡牌安全区外扩，不得侵入核心结论或正文。
- 长文必须放在稳定底板上，不直接压在复杂法阵或动态背景上。

## Elevation & Depth

层级主要由明暗、留白、细描边和一次性揭示高光建立。静态内容避免多层阴影和玻璃堆叠。卡牌容器可以拥有最强阴影和月晕；面板只使用克制的色调分层。阅读页禁止持续视差、强模糊和循环高光。

## Shapes

形状语言是柔和容器配细铭刻结构：卡牌容器稳定、面板边缘安静、按钮轮廓清楚。月相和几何承担仪式秩序，但不能变成装饰密度竞赛。圆角精确值在首个 Cocos 原型真机通过后固化；在此之前同类组件必须共用一个实现源。

## Components

### Foundational visual states

所有可操作组件必须具备默认、按下、选中、禁用、忙碌、成功、警告和错误状态。微信触屏不依赖 hover；键盘或桌面预览仍需可见焦点。忙碌态保持组件尺寸稳定，错误态不能只靠颜色表达。

### Buttons and actions

每屏只保留一个明确主操作。主按钮、次按钮和返回操作保持固定层级；保存、重新开始和返回不能同时获得相同强调。按钮换肤只能改变表皮，不能改变尺寸、位置和状态语义。

### Navigation and data display

Milestone 1 使用单场景状态流。顶部信息克制，流程状态由页面内容而非复杂导航表达。图鉴和历史按逻辑卡 ID 与日期呈现；未提供正式牌面时使用中性占位容器。

### Forms and overlays

问题类别选择器提供四个明确选项，默认“综合”。问题文本可跳过。结果卡预览使用产品自有覆盖层，关闭后恢复原位置；不使用浏览器式阻塞提醒。

### Iconography

使用单一细线图标体系，与铭刻分隔线共享克制线宽。正逆位、保存和隐私等关键含义必须带文字，不能只依赖图标。

### Motion

动效遵循“常驻慢、关键快；阅读时静、揭示时准”。抽牌决定发生在动效之前。翻牌结束后高光迅速收束。低特效模式关闭背景动画、粒子、多层视差和强发光，但保留状态反馈和流程完整性。

### Content and data visualization

文案语气平静、具体、非宿命。先给一句核心结论，再给解释和建议。危险问题使用安全响应，不用恐吓、确定性预言或专业诊断口吻。

## Do's and Don'ts

- **Do:** 让卡牌始终比背景、按钮、月相环和几何纹样更亮、更细致。
- **Do:** 使用月相、留白和短暂高光建立仪式节奏。
- **Do:** 在正式牌面未交付时使用统一中性占位，不创作“临时正式美术”。
- **Don't:** 用持续粒子、霓虹星云或复杂法阵填满屏幕。
- **Don't:** 允许主题改变布局、结果顺序、触控区或正文可读性。
- **Don't:** 让宋体或 Cinzel 进入长正文和高频按钮，牺牲中文小屏可读性。
